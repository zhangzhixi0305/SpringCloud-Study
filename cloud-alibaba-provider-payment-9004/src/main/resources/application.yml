server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #配置Nacos地址
        server-addr: 43.143.195.160:8848 #Nacos服务注册中心地址

management:
  endpoints:
    web:
      exposure:
        include: '*'
